---
description: Contextual modular configuration wizard that reads machine-profile.json and applies only the optimizations relevant to your detected hardware.
---

# /setup

Applies targeted system configurations and hardware optimizations based on the machine profile generated by `/init` (or `/scan`):
- Reads `~/.config/linux-wayland-suite/machine-profile.json` (runs automatic inspection if missing).
- Dynamically offers only the components detected on your system:
  * Keyboard layout & native cedilla (ABNT2 / US-intl)
  * Layout auto-heal protection against KWin reboot collapse bug
  * Smart Keyboard Power management for i8042 bus
  * Smart Wi-Fi Power management for AC vs Battery stability
  * Touchpad gestures (libinput-gestures)
  * Logitech MX Master 3S configuration (logid)
  * Display 60 Hz power-saver mode
  * Tongfang / Avell keyboard matrix unlock in GRUB

```bash
./bin/linux-wayland-config setup
```

---

## Guided Configuration Flow (Mandatory for AI Agents)

Before applying setup changes, the agent **must use `AskUserQuestion` (or `ask`)** to present detected components to the user:

### Question 1 — Components to Configure (multiSelect):
The agent reads `machine-profile.json` and presents only the detected options:
- **"Keyboard Layout and Native Cedilla"** — Recommended (ABNT2 / US-intl ç without input methods).
- **"Layout Auto-Heal on Login"** — Recommended (protects against KWin/Plasma layout collapse bug).
- **"Smart Wi-Fi Power Management"** — Recommended if laptop has Wi-Fi (disables radio sleep on AC).
- **"Smart Keyboard Power Management"** — Recommended if i8042 bus is present (anti-latch / zero latency).
- **"Touchpad Gestures (3/4 fingers)"** — Only if touchpad is present.
- **"Logitech MX Master 3S Mouse"** — Only if Logitech device is detected or requested.
- **"Set 60 Hz Display Refresh Rate"** — Only if 60 Hz mode is available and battery savings are desired.
- **"Tongfang / Avell Matrix Unlock (GRUB)"** — Only if Tongfang/Avell chassis is detected.

### Mapping Answers to Command Execution:

* **Apply all detected components:**
  ```bash
  ./bin/linux-wayland-config setup --all
  ```
* **Apply specific selections:**
  ```bash
  ./bin/linux-wayland-config setup --keyboard --autoheal --wifi-power
  ```
* **Interactive Terminal Wizard:**
  ```bash
  ./bin/linux-wayland-config setup
  ```

---

## Output Format (Mandatory across all tools)

Always report in these four phases, in this exact order:

### 1. Plan

Before executing any action:

- **Command:** exact command to be executed
- **Action:** summary of components to be configured
- **Reversible:** how to undo each modified component

### 2. Execution

One line per step with the corresponding result marker:

- `✅ <step>` — completed and verified
- `⏭️ <step>` — skipped (state reason)
- `⚠️ <step>` — completed with caveats / warning (state reason)
- `❌ <step>` — failed (include actual error output, never paraphrase)

### 3. Summary

Always at the end, even when no system state changed:

| Field | Content |
| :--- | :--- |
| Changed | objective list of changes, or `nothing — already compliant` |
| Unchanged | components skipped or declined by user |
| Backup | snapshot paths created for each component |
| Saved Report | `./bin/linux-wayland-config report` |
| How to Revert | `./bin/linux-wayland-config <component> --remove / --revert` |
| Requires | `re-login or reboot if layout / kernel parameters changed` |

### 4. Recommended Actions (Mandatory if ⚠️ or ❌ occurs)

Whenever **2. Execution** contains any item marked with `⚠️` (warning) or `❌` (failure), provide the exact 1-line command to fix each issue:

- `• <Issue description>`: `exact command to fix`
